# Linux-specific tweaks (Wayland/X11 safe)
linux_display_server         auto

# Notes:
# - If fast scrolling shows latency on X11+NVIDIA, consider setting sync_to_monitor no in kitty.conf
#   (trade tearing risk for lower latency).
# - For best clipboard behavior with some tools:
#   Wayland: install wl-clipboard; X11: xclip or xsel.
