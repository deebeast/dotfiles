# macOS-specific tweaks (ignored on Linux)
macos_titlebar_color         system

# Recommended toggles (uncomment if desired):
# Use Option as Alt for TUI apps needing Alt shortcuts
# macos_option_as_alt        both
# Faster transition than native animation
# macos_traditional_fullscreen yes
# Quit app when last window closes
# macos_quit_when_last_window_closed yes
